<div>
    <h5 class="challenge">
      <%= link_to challenge['title'], challenge_path(challenge['_id']) %> - <%= RepositoryChallenge.count_entries(challenge['_id']) %> entries
    </h5>
    <p><%= challenge['description'] %></p>
</div>
