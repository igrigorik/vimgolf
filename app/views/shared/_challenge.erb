<div>
    <h5 class="challenge">
      <%= link_to challenge['title'], challenge_path(challenge['_id']) %> - <%= challenge['count_entries'] %> entries
    </h5>
    <p><%= challenge['description'] %></p>
</div>
